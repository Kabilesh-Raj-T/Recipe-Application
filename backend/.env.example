# Replace <YOUR_PASSWORD> and the host below with your actual Supabase Connection URI.
# You can find this in Supabase: Settings -> Database -> Connection String (URI).
# Make sure to use the transaction mode string (port 6543).

DATABASE_URL="postgresql://postgres.fbegpohxligutiyeiair:<YOUR_PASSWORD>@aws-0-your-region.pooler.supabase.com:6543/postgres"
